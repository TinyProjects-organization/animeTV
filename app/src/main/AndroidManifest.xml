<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coders.animetv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".List.List"></activity>
        <activity
            android:name=".List.list"
            android:label="@string/title_activity_list"
            android:theme="@style/NoActionBar" />
        <activity android:name=".Chat.Chat" />
        <activity android:name=".Profile.Profile" />
        <activity android:name=".Login.ForgotPasswordActivity" />
        <activity android:name=".Homescreen.HomeScreen" />
        <activity android:name=".Login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>